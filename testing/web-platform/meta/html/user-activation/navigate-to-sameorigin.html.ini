[navigate-to-sameorigin.html]
  expected:
    if not debug and (os == "mac") and (version == "OS X 14.7.5"): [OK, TIMEOUT]
    if debug: [TIMEOUT, OK]
  [User activation propagation across a same-origin navigation]
    expected:
      if debug: [TIMEOUT, FAIL]
      [FAIL, TIMEOUT]
