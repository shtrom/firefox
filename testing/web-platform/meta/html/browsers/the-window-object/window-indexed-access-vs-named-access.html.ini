[window-indexed-access-vs-named-access.html]
  expected:
    if (os == "linux") and not tsan and fission and asan: [OK, TIMEOUT]
    if (os == "linux") and not tsan and not fission: [OK, TIMEOUT]
    if (os == "win") and debug: [OK, ERROR]
    if (os == "linux") and tsan: [OK, TIMEOUT]
